package com.xpenzo.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary palette (from redesign) ──────────────────────────────────────────
val Cream       = Color(0xFFFFF6EC)   // page background
val Surface     = Color(0xFFFFFFFF)   // cards
val Ink         = Color(0xFF2C2520)   // primary text
val Muted       = Color(0xFFA89B8C)   // secondary text

// Coral — primary brand / hero card
val Coral       = Color(0xFFFF6F61)
val CoralSoft   = Color(0xFFFFE4DF)

// Mint — success / budget
val Mint        = Color(0xFF2BC4A2)
val MintSoft    = Color(0xFFD8F5EE)

// Sun — warning / daily avg / alerts
val Sun         = Color(0xFFFFB23E)
val SunSoft     = Color(0xFFFFF0D6)

// Grape — AI / insights
val Grape       = Color(0xFF8B7BF0)
val GrapeSoft   = Color(0xFFEAE6FF)

// Sky — transport / info
val Sky         = Color(0xFF4DA8F0)
val SkySoft     = Color(0xFFDCEEFF)

// Error
val Error       = Color(0xFFE53935)
val ErrorSoft   = Color(0xFFFFEBEE)

// Category icon tints (used for the chips and category badges)
val CategoryFood        = Coral
val CategoryGroceries   = Mint
val CategoryTransport   = Sky
val CategoryShopping    = Grape
val CategoryHealth      = Color(0xFFEF5350)
val CategoryBills       = Sun
val CategoryOthers      = Muted
